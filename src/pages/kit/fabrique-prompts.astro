---
import Base from '../../layouts/Base.astro';
import { url } from '../../lib/paths';
---
<Base title="La fabrique à prompts">
  <section class="section container">
    <nav aria-label="Fil d'Ariane" class="kw-label">
      <a href={url('/kit')}>Le kit</a> · <a href={url('/kit#etape-boite-a-outils-ia')}>Étape 4</a> · La fabrique à prompts
    </nav>
    <h1>La fabrique à prompts</h1>
    <p class="prose">
      {/* TODO : explorateur des 6 fonctions/prompts du brief 05 + bouton copier (aria-live) + fiche imprimable — Phase 5. */}
      Cet outil interactif de formulation de prompts sera bientôt disponible.
    </p>
  </section>
</Base>

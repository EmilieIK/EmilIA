---
import Base from '../../layouts/Base.astro';
import { url } from '../../lib/paths';
---
<Base title="Aiguiser son regard">
  <section class="section container">
    <nav aria-label="Fil d'Ariane" class="kw-label">
      <a href={url('/kit')}>Le kit</a> · <a href={url('/kit#etape-regards-croises')}>Étape 3</a> · Aiguiser son regard
    </nav>
    <h1>Aiguiser son regard</h1>
    <p class="prose">
      {/* TODO : repérage d'erreurs interactif (catégories du brief 05), bingo / défi chrono, statut en texte — Phase 5. */}
      Cet outil interactif de repérage d'erreurs sera bientôt disponible.
    </p>
  </section>
</Base>

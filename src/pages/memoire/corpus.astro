---
import Base from '../../layouts/Base.astro';
import { url } from '../../lib/paths';
---
<Base title="Exploration du corpus">
  <section class="section container">
    <nav aria-label="Fil d'Ariane" class="kw-label">
      <a href={url('/memoire')}>Le mémoire</a> · Exploration du corpus
    </nav>
    <h1>Exploration du corpus</h1>
    <p class="prose">
      {/* TODO : sélecteur d'étudiant·e anonymisé·e, jet → réécritures → interactions IA → grille codée, bascule avant/après — Phase 7 du brief, alimentée par le corpus vierge anonymisé d'Émilie. */}
      Cette exploration sera bientôt disponible, à partir d'un corpus entièrement anonymisé.
    </p>
  </section>
</Base>
